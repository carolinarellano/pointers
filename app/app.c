#include "pointers.h"
int main(void)
{
    int array[30] = {4, 9, 4, 1, 23, 60, 8, 2, 81};

    //bubble_sort(array, 9);
    //insertion_sort(array, 9);
    //reverse(array, 9);
    //calulate_frequency(array, 9, 4);

    return 0;
}
